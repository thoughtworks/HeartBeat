FROM node:16-alpine AS builder
WORKDIR /app
COPY ./backend ./
RUN npm install -g yarn
RUN yarn install && yarn build

LABEL app=HeartBeat
LABEL arch=Backend
RUN mkdir -p ./csv
EXPOSE 3001
CMD [ "yarn", "host" ]